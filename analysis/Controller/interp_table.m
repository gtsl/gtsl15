function full_table = interp_table(table, dely, delx)

% [m, n] = size(table);
% m : x
% n : y

% Adds new space but doesn't expand existing table

% full_table = zeros(m * delx, n * dely);
% % full_table(delx*(1:m), dely*(1:n)) = table;
% F = griddedInterpolant(table);
% gvq = {1:1:m * delx,1:1:n * dely};
% full_table = F(gvq);


end