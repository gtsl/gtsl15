dt = 0.01;
tvec = 0:dt:20;
nSteps = length(tvec);
hmax = 3000;
hdotmax = 500;
table = zeros(hmax , hdot)
% Nominal path is size (2, nSteps). Table should include +- 100 for h and
% hdot. Flight software will default to switch controller when outside of 
% range.
% 
%
%

for n = 1:length(tvec)
    
end